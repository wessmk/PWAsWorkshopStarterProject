# PWAs GDG DevFest Workshop

### Installation

```sh
$ cd pwaWorkshopStartApp
$ npm install -g live-server
$ npm install -g json-server
$ npm install
$ npm run jsonserver
$ live-server --port=3000
```
